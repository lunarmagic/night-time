#pragma once

#include "math/math.h"

namespace night
{

	struct NIGHT_API intersect
	{
		// TODO: intersect planes 2d and 3d,
		// intersect lines 2d
		// intersect circles
		// intersect spheres
		// intersect rays 2d
		// intersect ellipses(difficult)
		// intersect triangles and quads
		// sphere-plane(return circle origin, direction and radius)
		// cylinder-plane(return ellipse origin, minor and major axes and radii)

		//static vec2 rays(vec2 const& ray_origin_1, vec2 const& ray_direction_1, vec2 const& ray_origin_2, vec2 const& ray_direction_2);
	};

}